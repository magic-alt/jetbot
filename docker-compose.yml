# Compose Spec — `version:` is intentionally omitted (obsolete in v2 Compose).

services:
  api:
    build:
      context: .
      args:
        EXTRAS: "celery,postgres,s3"
    ports:
      - "${JETBOT_API_PORT:-8000}:8000"
    env_file:
      - path: .env
        required: false
    environment:
      - TASK_BACKEND=celery
      - CELERY_BROKER_URL=redis://redis:6379/0
      - STORAGE_BACKEND=postgres
      - DATABASE_URL=postgresql://jetbot:jetbot@postgres:5432/jetbot
      - S3_ENDPOINT=http://minio:9000
    volumes:
      - app-data:/app/data
    depends_on:
      - redis
      - postgres
      - minio

  worker:
    build:
      context: .
      args:
        EXTRAS: "celery,postgres,s3"
    command: celery -A src.tasks worker --loglevel=info --concurrency=2
    env_file:
      - path: .env
        required: false
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - STORAGE_BACKEND=postgres
      - DATABASE_URL=postgresql://jetbot:jetbot@postgres:5432/jetbot
      - S3_ENDPOINT=http://minio:9000
    volumes:
      - app-data:/app/data
    depends_on:
      - redis
      - postgres

  redis:
    image: redis:7-alpine
    ports:
      - "${JETBOT_REDIS_PORT:-6379}:6379"

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: jetbot
      POSTGRES_PASSWORD: jetbot
      POSTGRES_DB: jetbot
    ports:
      - "${JETBOT_POSTGRES_PORT:-5432}:5432"
    volumes:
      - pg-data:/var/lib/postgresql/data

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: minio123
    ports:
      - "${JETBOT_MINIO_PORT:-9000}:9000"
      - "${JETBOT_MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio-data:/data

volumes:
  app-data:
  pg-data:
  minio-data:
